openapi: 3.1.0
info:
  title: ESP32-PixelCast REST API
  version: 0.2.0
  description: >
    REST API for controlling a HUB75 LED matrix panel (64x64) running on
    ESP32 Trinity. Manages custom apps, notifications, weather, trackers,
    indicators, icons, and system settings.


    **BREAKING (v0.2.0)**: sleep `sleep_until` / `until` epoch semantics
    changed from the device's local frame to UTC Unix timestamps. Clients
    that previously added the device's NTP offset before sending must stop
    doing so.
  license:
    name: MIT
    identifier: MIT

servers:
  - url: http://pixelcast.local/api
    description: mDNS (default)
  - url: http://{deviceIP}/api
    description: Direct IP
    variables:
      deviceIP:
        default: 192.168.1.100

tags:
  - name: System
    description: Device info, settings, brightness, reboot.
  - name: Apps
    description: Custom app management and rotation.
  - name: Weather
    description: WeatherClock system app.
  - name: Tracker
    description: Financial/metric trackers (crypto, stocks).
  - name: Gauge
    description: Progress-bar dashboards for bounded 0-100% values.
  - name: Notifications
    description: Notification queue management.
  - name: Indicators
    description: Corner indicator LEDs (1-3).
  - name: Icons
    description: Icon filesystem management and LaMetric downloads.
  - name: Sleep
    description: >
      Display sleep mode (schedule-based or manual override). Epoch values
      are standard UTC Unix timestamps (seconds since 1970-01-01 UTC); the
      device handles local time display via `ntp.tz_posix`. BREAKING in
      v0.2.0: previously these epochs were in the device's local frame.

paths:
  # ===========================================================================
  # System
  # ===========================================================================
  /stats:
    get:
      operationId: getStats
      summary: Get system statistics
      tags: [System]
      responses:
        "200":
          description: System statistics.
          content:
            application/json:
              schema:
                $ref: "schemas/stats.yaml#/StatsResponse"

  /settings:
    get:
      operationId: getSettings
      summary: Get current settings
      tags: [System]
      responses:
        "200":
          description: Current device settings.
          content:
            application/json:
              schema:
                $ref: "schemas/settings.yaml#/SettingsResponse"
    post:
      operationId: updateSettings
      summary: Update settings
      description: >
        Partial update: only provided fields are changed.
      tags: [System]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "schemas/settings.yaml#/SettingsUpdateRequest"
      responses:
        "200":
          description: Settings updated.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: >
            Invalid JSON, or `weatherDuration` outside the 3000-60000 ms range.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"

  /brightness:
    post:
      operationId: setBrightness
      summary: Set display brightness
      tags: [System]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [brightness]
              properties:
                brightness:
                  type: integer
                  minimum: 0
                  maximum: 255
                  description: Brightness value (0-255).
      responses:
        "200":
          description: Brightness updated.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: Missing brightness value.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"

  /reboot:
    post:
      operationId: reboot
      summary: Reboot device
      description: >
        Triggers a deferred reboot. The response is sent before the
        device restarts.
      tags: [System]
      responses:
        "200":
          description: Reboot initiated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    const: true
                  message:
                    type: string
                    examples:
                      - "Rebooting..."

  # ===========================================================================
  # Apps
  # ===========================================================================
  /apps:
    get:
      operationId: listApps
      summary: List all apps in rotation
      tags: [Apps]
      responses:
        "200":
          description: List of active apps.
          content:
            application/json:
              schema:
                $ref: "schemas/custom-app.yaml#/AppListResponse"

  /custom:
    post:
      operationId: createOrUpdateApp
      summary: Create or update a custom app
      description: >
        Supports single-zone (text/icon/color) or multi-zone (zones array)
        layouts. App name can be in query param or JSON body.
      tags: [Apps]
      parameters:
        - name: name
          in: query
          schema:
            type: string
          description: App name. Also accepted in JSON body.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "schemas/custom-app.yaml#/CustomAppRequest"
            examples:
              singleZone:
                summary: Single-zone app
                value:
                  text: "Hello World"
                  icon: "smiley"
                  color: "#FF8800"
                  duration: 10000
              multiZone:
                summary: Multi-zone dashboard
                value:
                  zones:
                    - text: "22.5C"
                      icon: "thermo"
                      label: "Salon"
                      color: "#FF8800"
                    - text: "58%"
                      icon: "humidity"
                      label: "Humidity"
                      color: "#00D4FF"
                  duration: 10000
              coloredSegments:
                summary: Colored text segments
                value:
                  text:
                    - t: "22.5"
                      c: "#FF8800"
                    - t: "C"
                      c: "#666666"
                  icon: "thermo"
      responses:
        "200":
          description: App created or updated.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: Missing name or invalid zones array.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
        "500":
          description: Failed to add app (no slot available).
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
    delete:
      operationId: deleteApp
      summary: Delete a custom app
      tags: [Apps]
      parameters:
        - name: name
          in: query
          required: true
          schema:
            type: string
          description: App name to delete.
      responses:
        "200":
          description: App deleted.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: Missing app name.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
        "404":
          description: App not found or is a system app.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"

  # ===========================================================================
  # Weather
  # ===========================================================================
  /weather:
    get:
      operationId: getWeather
      summary: Get current weather data
      tags: [Weather]
      responses:
        "200":
          description: Weather data (or valid=false if not yet received).
          content:
            application/json:
              schema:
                $ref: "schemas/weather.yaml#/WeatherResponse"
    post:
      operationId: updateWeather
      summary: Update weather data
      description: >
        Push current conditions, an optional 7-day forecast and an optional
        hourly window. The WeatherClock system app displays all three: the
        current conditions on top, then a carousel whose first page is the
        hourly chart, when a payload carries one, followed by the forecast days.
        It falls back to a simple clock when data is stale (>1 hour).
      tags: [Weather]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "schemas/weather.yaml#/WeatherUpdateRequest"
            examples:
              threeDayForecast:
                summary: Current + 3-day forecast + hourly window
                value:
                  current:
                    icon: "w_clear_day"
                    temp: 22
                    temp_min: 16
                    temp_max: 29
                    humidity: 50
                  forecast:
                    - day: "MON"
                      icon: "w_partly_day"
                      temp_min: 14
                      temp_max: 23
                    - day: "TUE"
                      icon: "w_rain"
                      temp_min: 10
                      temp_max: 17
                    - day: "WED"
                      icon: "w_snow"
                      temp_min: -2
                      temp_max: 3
                  today:
                    hours:
                      - { h: 15, temp: 22, pop: 0, precip: 0 }
                      - { h: 16, temp: 23, pop: 10, precip: 2 }
                      - { h: 17, temp: 23, pop: 40, precip: 8 }
                      - { h: 18, temp: 21, pop: 80, precip: 25 }
                      - { h: 19, temp: 19, pop: 30, precip: 4 }
                      - { h: 20, temp: 18, pop: 10, precip: 0 }
                    segments:
                      - { from: 15, to: 16, icon: "w_partly_day" }
                      - { from: 17, to: 19, icon: "w_rain" }
                      - { from: 20, to: 20, icon: "w_clear_night" }
      responses:
        "200":
          description: Weather data updated.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: Missing `current` object.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"

  # ===========================================================================
  # Tracker
  # ===========================================================================
  /trackers:
    get:
      operationId: listTrackers
      summary: List all active trackers
      tags: [Tracker]
      responses:
        "200":
          description: List of tracker summaries.
          content:
            application/json:
              schema:
                $ref: "schemas/tracker.yaml#/TrackerListResponse"

  /tracker:
    get:
      operationId: getTracker
      summary: Get single tracker data
      tags: [Tracker]
      parameters:
        - name: name
          in: query
          required: true
          schema:
            type: string
          description: Tracker identifier (e.g. "btc").
      responses:
        "200":
          description: Tracker data.
          content:
            application/json:
              schema:
                $ref: "schemas/tracker.yaml#/TrackerResponse"
        "400":
          description: Missing tracker name.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
        "404":
          description: Tracker not found.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
    post:
      operationId: createOrUpdateTracker
      summary: Create or update a tracker
      description: >
        Push tracker data (price, change%, sparkline). The tracker is
        automatically registered as an app in the rotation with ID
        `tracker_{name}`. Name can be in query param or JSON body.
      tags: [Tracker]
      parameters:
        - name: name
          in: query
          schema:
            type: string
          description: Tracker name. Also accepted in JSON body.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "schemas/tracker.yaml#/TrackerUpdateRequest"
            examples:
              btcTracker:
                summary: BTC tracker with sparkline
                value:
                  symbol: "BTC"
                  icon: "bitcoin"
                  currency: "USD"
                  value: 98452.30
                  change: 2.14
                  sparkline: [92100, 89300, 93200, 91800, 95400, 94100, 97600, 96200, 98452]
                  symbolColor: "#FF8800"
                  sparklineColor: "#00D4FF"
                  bottomText: "Vol 24h: 42B"
                  sparklinePeriod: "7d"
              fundTracker:
                summary: Fund with a scrolling name and a colored footer
                value:
                  symbol: "Amundi PEA Monde"
                  icon: "world"
                  currency: "EUR"
                  value: 34.82
                  change: 0.63
                  sparkline: [34100, 34300, 34200, 34500, 34700, 34600, 34820]
                  symbolColor: "#4CAF50"
                  bottomText:
                    - t: "MSCI "
                      c: "#888888"
                    - t: "World"
                      c: "#00D4FF"
      responses:
        "200":
          description: Tracker created or updated.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: Missing tracker name.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
        "500":
          description: No tracker slot available.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
    delete:
      operationId: deleteTracker
      summary: Delete a tracker
      description: Removes the tracker data and its app from rotation.
      tags: [Tracker]
      parameters:
        - name: name
          in: query
          required: true
          schema:
            type: string
          description: Tracker name to delete.
      responses:
        "200":
          description: Tracker deleted.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: Missing tracker name.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
        "404":
          description: Tracker not found.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"

  # ===========================================================================
  # Gauge
  # ===========================================================================
  /gauges:
    get:
      operationId: listGauges
      summary: List all active gauges
      tags: [Gauge]
      responses:
        "200":
          description: List of gauge summaries.
          content:
            application/json:
              schema:
                $ref: "schemas/gauge.yaml#/GaugeListResponse"

  /gauge:
    get:
      operationId: getGauge
      summary: Get single gauge data
      tags: [Gauge]
      parameters:
        - name: name
          in: query
          required: true
          schema:
            type: string
          description: Gauge identifier (e.g. "claude").
      responses:
        "200":
          description: Gauge data.
          content:
            application/json:
              schema:
                $ref: "schemas/gauge.yaml#/GaugeResponse"
        "400":
          description: Missing gauge name.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
        "404":
          description: Gauge not found.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
    post:
      operationId: createOrUpdateGauge
      summary: Create or update a gauge
      description: >
        Push a set of rows, each a bounded 0-100% value with its own progress bar.
        The gauge is automatically registered as an app in the rotation with ID
        `gauge_{name}`. Name can be in query param or JSON body. The update is
        partial: `title`, `icon` and `rows` are only written when present, and a
        `rows` array replaces the stored rows whole. Two gauges can exist at a time.
      tags: [Gauge]
      parameters:
        - name: name
          in: query
          schema:
            type: string
          description: Gauge name. Also accepted in JSON body.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "schemas/gauge.yaml#/GaugeUpdateRequest"
            examples:
              claudeLimits:
                summary: Subscription limits, four rows over two pages
                value:
                  title: "Claude"
                  icon: "claude"
                  duration: 10000
                  staleAfter: 2700
                  staleBehavior: "dim"
                  rows:
                    - label: "5h"
                      info: "14:50"
                      value: "41%"
                      percent: 41
                      color: "#4CAF50"
                      note: "x1.2^"
                      noteColor: "#FFC107"
                    - label: "7j"
                      info: "15/08 17h"
                      value: "28%"
                      percent: 28
                      color: "#4CAF50"
                      note: "x1.1>"
                      noteColor: "#4CAF50"
                    - label: "fable"
                      value: "3%"
                      percent: 3
                      color: "#4CAF50"
                    - label: "credits"
                      value: "1%"
                      percent: 1
                      color: "#4CAF50"
              diskUsage:
                summary: Disk fill levels, no info and no note, bars at full width
                value:
                  title: "Disques"
                  icon: "disk"
                  rows:
                    - label: "root"
                      value: "62%"
                      percent: 62
                      color: "#4CAF50"
                    - label: "data"
                      value: "88%"
                      percent: 88
                      color: "#FFC107"
                    - label: "backup"
                      value: "97%"
                      percent: 97
                      color: "#F44336"
      responses:
        "200":
          description: Gauge created or updated.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: >
            Missing gauge name, invalid JSON, a name over fifteen characters
            (`Gauge name too long (max 15)`), more than nine rows
            (`Too many rows (max 9)`), or an unknown `staleBehavior`. Nothing is
            written unless the whole payload is accepted.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
        "500":
          description: No gauge slot available.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
    delete:
      operationId: deleteGauge
      summary: Delete a gauge
      description: Removes the gauge data and its app from rotation.
      tags: [Gauge]
      parameters:
        - name: name
          in: query
          required: true
          schema:
            type: string
          description: Gauge name to delete.
      responses:
        "200":
          description: Gauge deleted.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: Missing gauge name.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
        "404":
          description: Gauge not found.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"

  # ===========================================================================
  # Sleep
  # ===========================================================================
  /sleep:
    get:
      operationId: getSleep
      summary: Get sleep state and configuration
      description: >
        Returns the current sleep state (active or not, reason, override
        expiry) and the full sleep configuration snapshot.
      tags: [Sleep]
      responses:
        "200":
          description: Current sleep state and configuration.
          content:
            application/json:
              schema:
                $ref: "schemas/sleep.yaml#/SleepState"
              examples:
                awake:
                  summary: Device is awake
                  value:
                    sleeping: false
                    config:
                      enabled: true
                      display_mode: "black"
                      schedule:
                        monday:
                          all_day: false
                          slots:
                            - start: "20:00"
                              end: "07:00"
                      sleep_until: 0
                overrideActive:
                  summary: Manual override active
                  value:
                    sleeping: true
                    reason: "override"
                    until: 4102444800
                    config:
                      enabled: true
                      display_mode: "clock"
                      schedule: {}
                      sleep_until: 4102444800
                scheduleActive:
                  summary: Sleeping because the weekly schedule matches now
                  value:
                    sleeping: true
                    reason: "schedule"
                    config:
                      enabled: true
                      display_mode: "black"
                      schedule:
                        monday:
                          all_day: false
                          slots:
                            - start: "20:00"
                              end: "07:00"
                      sleep_until: 0
                ntpNotSynced:
                  summary: NTP not yet synced (fail-open, schedule not evaluated)
                  value:
                    sleeping: false
                    reason: "ntp_not_synced"
                    config:
                      enabled: true
                      display_mode: "black"
                      schedule: {}
                      sleep_until: 0
    post:
      operationId: updateSleep
      summary: Update sleep configuration (partial)
      description: >
        Partial update of the sleep configuration. Only provided fields are
        changed; omitted fields are left untouched. Rejected payloads do
        not partially mutate state (atomic apply).


        Validation errors returned as `400` with `{"error": "<message>"}`
        include: `Invalid JSON`, `Invalid display_mode`,
        `sleep_until is in the past`, `Unknown day: <name>`,
        `Invalid time format`, `Too many slots`.
      tags: [Sleep]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "schemas/sleep.yaml#/SleepConfig"
            examples:
              enableSchedule:
                summary: Enable schedule with one weeknight slot
                value:
                  enabled: true
                  display_mode: "black"
                  schedule:
                    monday:
                      all_day: false
                      slots:
                        - start: "20:00"
                          end: "07:00"
              setOverride:
                summary: Manual override (sleep until epoch)
                value:
                  sleep_until: 4102444800
              clearOverride:
                summary: Clear manual override
                value:
                  sleep_until: 0
      responses:
        "200":
          description: Sleep configuration updated.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: Invalid payload (see description for full error list).
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"

  /sleep/wake:
    post:
      operationId: wake
      summary: Clear sleep override
      description: >
        Clears any active `sleep_until` override and wakes the device
        immediately. Equivalent to `POST /api/sleep` with `{"sleep_until": 0}`.
        Schedule-based sleep is not affected.
      tags: [Sleep]
      responses:
        "200":
          description: Override cleared.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"

  # ===========================================================================
  # Notifications
  # ===========================================================================
  /notify:
    post:
      operationId: sendNotification
      summary: Send a notification
      description: >
        Push a notification to the display. Notifications interrupt app
        rotation and are displayed with a card frame overlay.
      tags: [Notifications]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "schemas/notification.yaml#/NotificationRequest"
            examples:
              simple:
                summary: Simple notification
                value:
                  text: "New message!"
                  icon: "mail"
                  color: "#0096FF"
                  duration: 5000
              urgent:
                summary: Urgent notification
                value:
                  text: "Alert!"
                  icon: "warning"
                  color: "#FF0000"
                  urgent: true
                  hold: true
      responses:
        "200":
          description: Notification queued.
          content:
            application/json:
              schema:
                $ref: "schemas/notification.yaml#/NotificationResponse"
        "400":
          description: Missing text or invalid JSON.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
        "503":
          description: Notification queue full.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"

  /notify/list:
    get:
      operationId: listNotifications
      summary: List active notifications
      tags: [Notifications]
      responses:
        "200":
          description: Active notification queue.
          content:
            application/json:
              schema:
                $ref: "schemas/notification.yaml#/NotificationListResponse"

  /notify/dismiss:
    post:
      operationId: dismissNotification
      summary: Dismiss current notification
      tags: [Notifications]
      responses:
        "200":
          description: Notification dismissed.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "404":
          description: No active notification.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"

  # ===========================================================================
  # Indicators
  # ===========================================================================
  /indicator1:
    post:
      operationId: setIndicator1
      summary: Set indicator 1
      tags: [Indicators]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "schemas/indicator.yaml#/IndicatorRequest"
            examples:
              solidRed:
                summary: Solid red
                value:
                  mode: "solid"
                  color: "#FF0000"
              blinkGreen:
                summary: Blinking green
                value:
                  mode: "blink"
                  color: "#00FF00"
                  blinkInterval: 500
      responses:
        "200":
          description: Indicator set.
          content:
            application/json:
              schema:
                $ref: "schemas/indicator.yaml#/IndicatorResponse"
        "400":
          description: Invalid mode.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
    delete:
      operationId: clearIndicator1
      summary: Turn off indicator 1
      tags: [Indicators]
      responses:
        "200":
          description: Indicator turned off.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    const: true
                  mode:
                    type: string
                    const: "off"

  /indicator2:
    post:
      operationId: setIndicator2
      summary: Set indicator 2
      tags: [Indicators]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "schemas/indicator.yaml#/IndicatorRequest"
      responses:
        "200":
          description: Indicator set.
          content:
            application/json:
              schema:
                $ref: "schemas/indicator.yaml#/IndicatorResponse"
        "400":
          description: Invalid mode.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
    delete:
      operationId: clearIndicator2
      summary: Turn off indicator 2
      tags: [Indicators]
      responses:
        "200":
          description: Indicator turned off.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    const: true
                  mode:
                    type: string
                    const: "off"

  /indicator3:
    post:
      operationId: setIndicator3
      summary: Set indicator 3
      tags: [Indicators]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "schemas/indicator.yaml#/IndicatorRequest"
      responses:
        "200":
          description: Indicator set.
          content:
            application/json:
              schema:
                $ref: "schemas/indicator.yaml#/IndicatorResponse"
        "400":
          description: Invalid mode.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
    delete:
      operationId: clearIndicator3
      summary: Turn off indicator 3
      tags: [Indicators]
      responses:
        "200":
          description: Indicator turned off.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    const: true
                  mode:
                    type: string
                    const: "off"

  # ===========================================================================
  # Icons
  # ===========================================================================
  /icons:
    get:
      operationId: listIcons
      summary: List all icons on filesystem
      tags: [Icons]
      responses:
        "200":
          description: Icon list with storage info.
          content:
            application/json:
              schema:
                $ref: "schemas/icon.yaml#/IconListResponse"
    post:
      operationId: uploadIcon
      summary: Upload an icon (PNG or GIF)
      description: >
        Upload a PNG or GIF icon to the device filesystem. Recommended
        sizes: 8x8 or 16x16 pixels.
      tags: [Icons]
      parameters:
        - name: name
          in: query
          required: true
          schema:
            type: string
          description: Icon name (used as filename without extension).
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG or GIF file.
      responses:
        "200":
          description: Icon uploaded.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: Upload failed (invalid format or size).
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
    delete:
      operationId: deleteIcon
      summary: Delete an icon
      tags: [Icons]
      parameters:
        - name: name
          in: query
          required: true
          schema:
            type: string
          description: Icon name to delete.
      responses:
        "200":
          description: Icon deleted.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: Missing name parameter.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
        "404":
          description: Icon not found.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"

  /icons/{name}:
    get:
      operationId: getIcon
      summary: Serve an icon file
      description: Returns the raw PNG or GIF image file.
      tags: [Icons]
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
            pattern: "^[a-zA-Z0-9_-]+$"
          description: Icon name (without extension).
      responses:
        "200":
          description: Icon image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
            image/gif:
              schema:
                type: string
                format: binary
        "404":
          description: Icon not found.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"

  /icons/lametric:
    post:
      operationId: downloadLaMetricIcon
      summary: Download icon from LaMetric
      description: >
        Downloads an icon from the LaMetric icon database and saves it to
        the device filesystem.
      tags: [Icons]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "schemas/icon.yaml#/LaMetricRequest"
      responses:
        "200":
          description: Icon downloaded.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/SuccessResponse"
        "400":
          description: Missing or invalid icon ID.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
        "500":
          description: Download failed.
          content:
            application/json:
              schema:
                $ref: "schemas/common.yaml#/ErrorResponse"
