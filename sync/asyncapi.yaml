asyncapi: 3.0.0
info:
  title: ESP32-PixelCast MQTT API
  version: 0.2.0
  description: >
    MQTT interface for controlling a HUB75 LED matrix panel (64x64) running
    on ESP32 Trinity. JSON payloads mirror the REST API. MQTT must be enabled
    via `POST /api/settings` or the settings file.


    **Key differences from REST:**

    - Fire-and-forget: no response (errors logged to Serial only).

    - Delete via `{"delete": true}` flag instead of HTTP DELETE.

    - Name can be in topic path or JSON body for custom/tracker/gauge.

    - No icon management over MQTT.

    **BREAKING (v0.2.0)**: sleep `until` epoch semantics changed from the
    device's local frame to UTC Unix timestamps. Clients that previously
    added the device's NTP offset before sending must stop doing so.
  license:
    name: MIT

defaultContentType: application/json

servers:
  mosquitto:
    host: "{brokerHost}:{brokerPort}"
    protocol: mqtt
    description: MQTT broker (e.g. Mosquitto).
    variables:
      brokerHost:
        default: localhost
      brokerPort:
        default: "1883"
    bindings:
      mqtt:
        clientId: pixelcast_{MAC}
        keepAlive: 60
        cleanSession: true
        lastWill:
          topic: "{prefix}/status"
          qos: 0
          retain: true
          message: "offline"

channels:
  # ===========================================================================
  # Commands (receive from clients)
  # ===========================================================================
  custom:
    address: "{prefix}/custom"
    description: >
      Create or update a custom app. App name must be in the JSON body.
      Send `{"delete": true, "name": "appName"}` to delete.
    parameters:
      prefix:
        description: MQTT topic prefix (default "pixelcast").
        default: pixelcast
    messages:
      customAppMessage:
        $ref: "#/components/messages/CustomApp"

  customNamed:
    address: "{prefix}/custom/{name}"
    description: >
      Create or update a custom app with name in the topic path.
      Send `{"delete": true}` to delete.
    parameters:
      prefix:
        description: MQTT topic prefix.
        default: pixelcast
      name:
        description: App name.
    messages:
      customAppMessage:
        $ref: "#/components/messages/CustomApp"

  notify:
    address: "{prefix}/notify"
    description: Send a notification to the display.
    parameters:
      prefix:
        default: pixelcast
    messages:
      notificationMessage:
        $ref: "#/components/messages/Notification"

  dismiss:
    address: "{prefix}/dismiss"
    description: Dismiss the current notification. Payload can be empty.
    parameters:
      prefix:
        default: pixelcast
    messages:
      dismissMessage:
        $ref: "#/components/messages/Empty"

  indicator1:
    address: "{prefix}/indicator1"
    description: Set corner indicator 1. Send `{"mode":"off"}` to turn off.
    parameters:
      prefix:
        default: pixelcast
    messages:
      indicatorMessage:
        $ref: "#/components/messages/Indicator"

  indicator2:
    address: "{prefix}/indicator2"
    description: Set corner indicator 2.
    parameters:
      prefix:
        default: pixelcast
    messages:
      indicatorMessage:
        $ref: "#/components/messages/Indicator"

  indicator3:
    address: "{prefix}/indicator3"
    description: Set corner indicator 3.
    parameters:
      prefix:
        default: pixelcast
    messages:
      indicatorMessage:
        $ref: "#/components/messages/Indicator"

  brightness:
    address: "{prefix}/brightness"
    description: Set display brightness.
    parameters:
      prefix:
        default: pixelcast
    messages:
      brightnessMessage:
        $ref: "#/components/messages/Brightness"

  settings:
    address: "{prefix}/settings"
    description: >
      Update device settings (partial update). Same payload as
      `POST /api/settings`, except that out-of-range values are clamped to their
      bounds instead of rejected, since MQTT has no response channel.
    parameters:
      prefix:
        default: pixelcast
    messages:
      settingsMessage:
        $ref: "#/components/messages/Settings"

  weather:
    address: "{prefix}/weather"
    description: >
      Update weather data for the WeatherClock system app.
    parameters:
      prefix:
        default: pixelcast
    messages:
      weatherMessage:
        $ref: "#/components/messages/Weather"

  tracker:
    address: "{prefix}/tracker"
    description: >
      Create or update a tracker. Name must be in the JSON body.
      Send `{"delete": true, "name": "btc"}` to delete.
    parameters:
      prefix:
        default: pixelcast
    messages:
      trackerMessage:
        $ref: "#/components/messages/Tracker"

  trackerNamed:
    address: "{prefix}/tracker/{name}"
    description: >
      Create or update a tracker with name in the topic path.
      Send `{"delete": true}` to delete.
    parameters:
      prefix:
        default: pixelcast
      name:
        description: Tracker name (e.g. "btc").
    messages:
      trackerMessage:
        $ref: "#/components/messages/Tracker"

  gauge:
    address: "{prefix}/gauge"
    description: >
      Create or update a gauge. Name must be in the JSON body.
      Send `{"delete": true, "name": "claude"}` to delete.
    parameters:
      prefix:
        default: pixelcast
    messages:
      gaugeMessage:
        $ref: "#/components/messages/Gauge"

  gaugeNamed:
    address: "{prefix}/gauge/{name}"
    description: >
      Create or update a gauge with name in the topic path.
      Send `{"delete": true}` to delete.
    parameters:
      prefix:
        default: pixelcast
      name:
        description: Gauge name (e.g. "claude").
    messages:
      gaugeMessage:
        $ref: "#/components/messages/Gauge"

  reboot:
    address: "{prefix}/reboot"
    description: Reboot the device. Payload can be empty.
    parameters:
      prefix:
        default: pixelcast
    messages:
      rebootMessage:
        $ref: "#/components/messages/Empty"

  sleep:
    address: "{prefix}/sleep"
    description: >
      Set a manual sleep override. Payload is `{"until": <epoch>}` where the
      epoch is a UTC Unix timestamp (seconds since 1970-01-01 UTC).
      Equivalent to the REST call `POST /api/sleep` with
      `{"sleep_until": <epoch>}`. Past values are rejected (logged to serial;
      state unchanged).
    parameters:
      prefix:
        default: pixelcast
    messages:
      sleepOverrideMessage:
        $ref: "#/components/messages/SleepOverride"

  wake:
    address: "{prefix}/wake"
    description: >
      Clear the manual sleep override and wake the device immediately.
      Payload can be empty. Equivalent to `POST /api/sleep/wake`.
    parameters:
      prefix:
        default: pixelcast
    messages:
      wakeMessage:
        $ref: "#/components/messages/Wake"

  # ===========================================================================
  # Status (published by device)
  # ===========================================================================
  status:
    address: "{prefix}/status"
    description: >
      Connection status (LWT). Published as retained. "online" on connect,
      "offline" as Last Will on disconnect.
    parameters:
      prefix:
        default: pixelcast
    messages:
      statusMessage:
        $ref: "#/components/messages/Status"

  stats:
    address: "{prefix}/stats"
    description: >
      System statistics published every 60 seconds. Not retained.
    parameters:
      prefix:
        default: pixelcast
    messages:
      statsMessage:
        $ref: "#/components/messages/Stats"

operations:
  # Commands (device receives)
  receiveCustom:
    action: receive
    channel:
      $ref: "#/channels/custom"
  receiveCustomNamed:
    action: receive
    channel:
      $ref: "#/channels/customNamed"
  receiveNotify:
    action: receive
    channel:
      $ref: "#/channels/notify"
  receiveDismiss:
    action: receive
    channel:
      $ref: "#/channels/dismiss"
  receiveIndicator1:
    action: receive
    channel:
      $ref: "#/channels/indicator1"
  receiveIndicator2:
    action: receive
    channel:
      $ref: "#/channels/indicator2"
  receiveIndicator3:
    action: receive
    channel:
      $ref: "#/channels/indicator3"
  receiveBrightness:
    action: receive
    channel:
      $ref: "#/channels/brightness"
  receiveSettings:
    action: receive
    channel:
      $ref: "#/channels/settings"
  receiveWeather:
    action: receive
    channel:
      $ref: "#/channels/weather"
  receiveTracker:
    action: receive
    channel:
      $ref: "#/channels/tracker"
  receiveTrackerNamed:
    action: receive
    channel:
      $ref: "#/channels/trackerNamed"
  receiveGauge:
    action: receive
    channel:
      $ref: "#/channels/gauge"
  receiveGaugeNamed:
    action: receive
    channel:
      $ref: "#/channels/gaugeNamed"
  receiveReboot:
    action: receive
    channel:
      $ref: "#/channels/reboot"
  receiveSleep:
    action: receive
    channel:
      $ref: "#/channels/sleep"
  receiveWake:
    action: receive
    channel:
      $ref: "#/channels/wake"

  # Status (device publishes)
  publishStatus:
    action: send
    channel:
      $ref: "#/channels/status"
    bindings:
      mqtt:
        retain: true
  publishStats:
    action: send
    channel:
      $ref: "#/channels/stats"

components:
  messages:
    CustomApp:
      payload:
        $ref: "schemas/custom-app.yaml#/CustomAppRequest"
      examples:
        - name: singleZone
          summary: Simple text app
          payload:
            text: "Hello!"
            icon: "smiley"
            color: "#FF0000"
        - name: multiZone
          summary: Two-zone dashboard
          payload:
            zones:
              - text: "22.5C"
                icon: "thermo"
                label: "Salon"
                color: "#FF8800"
              - text: "58%"
                icon: "humidity"
                color: "#00D4FF"
        - name: delete
          summary: Delete an app
          payload:
            delete: true
            name: "hello"

    Notification:
      payload:
        $ref: "schemas/notification.yaml#/NotificationRequest"
      examples:
        - name: alert
          summary: Urgent notification
          payload:
            text: "Alert!"
            icon: "warning"
            color: "#FF0000"
            urgent: true

    Indicator:
      payload:
        $ref: "schemas/indicator.yaml#/IndicatorRequest"
      examples:
        - name: blinkGreen
          summary: Blinking green
          payload:
            mode: "blink"
            color: "#00FF00"
            blinkInterval: 500
        - name: off
          summary: Turn off
          payload:
            mode: "off"

    Brightness:
      payload:
        type: object
        required: [brightness]
        properties:
          brightness:
            type: integer
            minimum: 0
            maximum: 255
      examples:
        - name: setBrightness
          payload:
            brightness: 50

    Settings:
      payload:
        $ref: "schemas/settings.yaml#/SettingsUpdateRequest"
      examples:
        - name: updateSettings
          payload:
            brightness: 100
            autoRotate: true
            defaultDuration: 15000
            weatherDuration: 12000

    Weather:
      payload:
        $ref: "schemas/weather.yaml#/WeatherUpdateRequest"
      examples:
        - name: currentAndForecast
          payload:
            current:
              icon: "w_clear_day"
              temp: 22
              temp_min: 16
              temp_max: 29
              humidity: 50
            forecast:
              - day: "MON"
                icon: "w_partly_day"
                temp_min: 14
                temp_max: 23
              - day: "TUE"
                icon: "w_rain"
                temp_min: 10
                temp_max: 17
            today:
              hours:
                - { h: 15, temp: 22, pop: 0, precip: 0 }
                - { h: 16, temp: 23, pop: 10, precip: 2 }
                - { h: 17, temp: 23, pop: 40, precip: 8 }
                - { h: 18, temp: 21, pop: 80, precip: 25 }
              segments:
                - { from: 15, to: 16, icon: "w_partly_day" }
                - { from: 17, to: 18, icon: "w_rain" }

    Tracker:
      payload:
        $ref: "schemas/tracker.yaml#/TrackerUpdateRequest"
      examples:
        - name: btcTracker
          summary: BTC price update
          payload:
            symbol: "BTC"
            icon: "bitcoin"
            currency: "USD"
            value: 98452.30
            change: 2.14
            sparkline: [92100, 89300, 93200, 91800, 95400]
            volumes: [3.2, 4.1, 3.8, 5.6, 4.9]
            sparklineRef: "open"
            symbolColor: "#FF8800"
            bottomText: "ATH 73.6K 03/24"
            sparklinePeriod: "7D"
        - name: fundTracker
          summary: Fund with a scrolling name and a colored footer
          payload:
            symbol: "Amundi PEA Monde"
            icon: "world"
            currency: "EUR"
            value: 34.82
            change: 0.63
            symbolColor: "#4CAF50"
            bottomText:
              - t: "MSCI "
                c: "#888888"
              - t: "World"
                c: "#00D4FF"
        - name: deleteTracker
          summary: Delete a tracker
          payload:
            delete: true

    Gauge:
      payload:
        $ref: "schemas/gauge.yaml#/GaugeUpdateRequest"
      description: >
        Same payload as `POST /api/gauge`, except that a name over fifteen characters
        and an oversized `rows` array are dropped with a serial log instead of an
        HTTP 400, and an unknown `staleBehavior` falls back to `dim` instead of being
        rejected. Watch the
        payload size: the MQTT receive buffer is 2048 bytes (`MQTT_BUFFER_SIZE`) and
        nine rows come close to 1.3 KB, so a broker message longer than the buffer is
        dropped before the firmware ever parses it.
      examples:
        - name: claudeLimits
          summary: Subscription limits, four rows over two pages
          payload:
            title: "Claude"
            icon: "claude"
            duration: 10000
            staleAfter: 2700
            staleBehavior: "dim"
            rows:
              - { label: "5h", info: "14:50", value: "41%", percent: 41, color: "#4CAF50", note: "x1.2^", noteColor: "#FFC107" }
              - { label: "7j", info: "15/08 17h", value: "28%", percent: 28, color: "#4CAF50", note: "x1.1>", noteColor: "#4CAF50" }
              - { label: "fable", value: "3%", percent: 3, color: "#4CAF50" }
              - { label: "credits", value: "1%", percent: 1, color: "#4CAF50" }
        - name: diskUsage
          summary: Disk fill levels, no info and no note, bars at full width
          payload:
            title: "Disques"
            icon: "disk"
            rows:
              - { label: "root", value: "62%", percent: 62, color: "#4CAF50" }
              - { label: "data", value: "88%", percent: 88, color: "#FFC107" }
              - { label: "backup", value: "97%", percent: 97, color: "#F44336" }
        - name: deleteGauge
          summary: Delete a gauge
          payload:
            delete: true

    Status:
      payload:
        type: string
        enum: ["online", "offline"]
      examples:
        - name: online
          payload: "online"
        - name: offline
          payload: "offline"

    Stats:
      payload:
        $ref: "schemas/stats.yaml#/MqttStatsPayload"
      examples:
        - name: stats
          payload:
            uptime: 3600
            freeHeap: 120000
            brightness: 128
            rssi: -45
            appCount: 3
            version: "0.1.0-dev"
            currentApp: "weather_clock"

    SleepOverride:
      payload:
        type: object
        required: [until]
        properties:
          until:
            type: integer
            minimum: 0
            description: >
              UTC Unix timestamp when the override expires (seconds since
              1970-01-01 UTC). Past values are rejected.
            examples:
              - 4102444800
      examples:
        - name: oneHourOverride
          summary: Sleep until a specific epoch
          payload:
            until: 4102444800

    Wake:
      payload:
        type: "null"
      description: >
        Wake-now command. Payload is ignored. Clears the active sleep
        override; schedule-based sleep is not affected.

    Empty:
      payload:
        type: "null"
      description: Empty payload (or any payload - ignored).
