{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://raw.githubusercontent.com/nicolas-codemate/pixelcast-client/main/pixelcast.schema.json",
    "title": "PixelCast client configuration",
    "description": "Contract of pixelcast.yaml. API keys are never written here, they are read from the environment.",
    "type": "object",
    "required": [
        "syncs"
    ],
    "additionalProperties": false,
    "properties": {
        "device": {
            "$ref": "#/definitions/device"
        },
        "syncs": {
            "description": "Sync groups, keyed by sync type. Omitting a group is the same as disabling it.",
            "type": "object",
            "minProperties": 1,
            "additionalProperties": false,
            "properties": {
                "weather": {
                    "$ref": "#/definitions/weatherSync"
                },
                "coingecko": {
                    "$ref": "#/definitions/trackerSync"
                },
                "twelvedata": {
                    "$ref": "#/definitions/trackerSync"
                },
                "boursorama": {
                    "$ref": "#/definitions/trackerSync"
                },
                "claude": {
                    "$ref": "#/definitions/claudeSync"
                },
                "github": {
                    "$ref": "#/definitions/githubSync"
                }
            }
        },
        "sleep": {
            "$ref": "#/definitions/deviceSleep"
        }
    },
    "definitions": {
        "device": {
            "description": "The device itself rather than what is shown on it. Every key of this section is optional, and a file without the section behaves exactly as before. The panel settings below are stored by the device and pushed only by `bin/console app:device:settings`, never by the scheduler, so editing them here changes nothing on the screen until that command has run. The one exception is brightnessWindows: the device holds no schedule of levels, so the scheduler walks the windows itself and pushes brightness while the client runs.",
            "type": "object",
            "minProperties": 1,
            "additionalProperties": false,
            "properties": {
                "timezone": {
                    "description": "Timezone the device is set to, e.g. \"Europe/Paris\". It is what sleep.timezone and every activeWindow.timezone fall back on, so the timezone is written once here instead of being copied into each section. It is also what the POSIX timezone pushed in ntp is derived from, which is why that one is never written by hand.",
                    "type": "string",
                    "minLength": 1
                },
                "brightness": {
                    "description": "How bright the panel is, 0 the dimmest and 255 the brightest. Without this key the device keeps the level it already holds. With brightnessWindows below it becomes the level held outside every window, and is then required.",
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 255
                },
                "brightnessWindows": {
                    "description": "Stretches of the day the panel is held at a level of its own, brightness applying outside them. They require brightness and timezone above. The device knows no schedule of levels, so the client is the one that applies them: a stopped client leaves the panel on the last level pushed.",
                    "type": "array",
                    "minItems": 1,
                    "items": {
                        "$ref": "#/definitions/brightnessWindow"
                    }
                },
                "autoRotate": {
                    "description": "Whether the device moves from one app to the next on its own. Without this key the device keeps what it already holds.",
                    "type": "boolean"
                },
                "defaultDuration": {
                    "description": "How long an app stays on the screen before the rotation moves on, in milliseconds. Without this key the device keeps what it already holds.",
                    "type": "integer",
                    "minimum": 1
                },
                "weatherDuration": {
                    "description": "How long the weather app stays on the screen, in milliseconds. It splits this duration between its own pages, so it needs more room than a plain app: 3000 at least, 60000 at most.",
                    "type": "integer",
                    "minimum": 3000,
                    "maximum": 60000
                },
                "ntp": {
                    "description": "Where the device reads the time it applies the sleep schedule in. The POSIX timezone that goes with it is derived from device.timezone and never declared here.",
                    "type": "object",
                    "required": [
                        "server"
                    ],
                    "additionalProperties": false,
                    "properties": {
                        "server": {
                            "description": "Host name of the NTP server the device asks the time of, e.g. \"pool.ntp.org\".",
                            "type": "string",
                            "minLength": 1
                        }
                    }
                }
            }
        },
        "deviceSleep": {
            "description": "Hours during which the device turns its panel off. The panel is pushed on demand by `bin/console app:device:sleep`, never by the scheduler, so the screen keeps whatever schedule it already holds until that command has run. The client suspends its own cycles from this section alone: no sync group is scheduled while the panel is off, and every enabled one pushes again at the instant it comes back on.",
            "type": "object",
            "required": [
                "enabled",
                "windows"
            ],
            "additionalProperties": false,
            "properties": {
                "enabled": {
                    "description": "Master toggle of the schedule on the device. A disabled schedule leaves the panel on.",
                    "type": "boolean"
                },
                "timezone": {
                    "description": "Timezone the window bounds are read in, e.g. \"Europe/Paris\". Without it device.timezone applies, and one of the two is required as soon as the schedule is enabled. Name the timezone the device itself is set to: it applies the schedule in its own local time, which the container clock does not share.",
                    "type": "string",
                    "minLength": 1
                },
                "displayMode": {
                    "description": "What the panel shows while asleep. \"black\" blanks it, \"clock\" leaves a dimmed clock. Without it the panel is blanked.",
                    "type": "string",
                    "enum": [
                        "black",
                        "clock"
                    ]
                },
                "days": {
                    "description": "Days the panel goes off. Without it every day is covered. A day left out is sent to the device with no window at all, so it clears whatever that day held.",
                    "type": "array",
                    "minItems": 1,
                    "uniqueItems": true,
                    "items": {
                        "type": "string",
                        "enum": [
                            "mon",
                            "tue",
                            "wed",
                            "thu",
                            "fri",
                            "sat",
                            "sun"
                        ]
                    }
                },
                "windows": {
                    "description": "Stretches of the day the panel stays off, the same for every declared day. The firmware takes at most two per day.",
                    "type": "array",
                    "minItems": 1,
                    "maxItems": 2,
                    "items": {
                        "$ref": "#/definitions/sleepWindow"
                    }
                }
            }
        },
        "sleepWindow": {
            "type": "object",
            "required": [
                "from",
                "to"
            ],
            "additionalProperties": false,
            "properties": {
                "from": {
                    "description": "Time the panel goes off, as HH:MM.",
                    "type": "string",
                    "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$"
                },
                "to": {
                    "description": "Time the panel comes back on, as HH:MM. A \"to\" earlier than \"from\" runs past midnight into the next day, the opposite of activeWindow.to, which refuses it.",
                    "type": "string",
                    "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$"
                }
            }
        },
        "brightnessWindow": {
            "description": "One stretch of the day the panel is held at a level of its own. Two windows covering the same minute are not an error: the last one declared in the file wins.",
            "type": "object",
            "required": [
                "from",
                "to",
                "level"
            ],
            "additionalProperties": false,
            "properties": {
                "from": {
                    "description": "Time the level starts applying, as HH:MM, included.",
                    "type": "string",
                    "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$"
                },
                "to": {
                    "description": "Time the level stops applying, as HH:MM, excluded, so two windows written back to back never dispute a minute. A \"to\" earlier than \"from\" runs past midnight into the next day, as in sleepWindow.to.",
                    "type": "string",
                    "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$"
                },
                "level": {
                    "description": "How bright the panel is during the window, 0 the dimmest and 255 the brightest.",
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 255
                },
                "days": {
                    "description": "Days the window applies on. Without it every day is covered. A window running past midnight is anchored on the day it starts.",
                    "type": "array",
                    "minItems": 1,
                    "uniqueItems": true,
                    "items": {
                        "type": "string",
                        "enum": [
                            "mon",
                            "tue",
                            "wed",
                            "thu",
                            "fri",
                            "sat",
                            "sun"
                        ]
                    }
                }
            }
        },
        "weatherSync": {
            "description": "Weather forecast pushed to the device.",
            "type": "object",
            "required": [
                "enabled",
                "interval",
                "latitude",
                "longitude",
                "units",
                "locale"
            ],
            "additionalProperties": false,
            "properties": {
                "enabled": {
                    "description": "A disabled group is never scheduled and cannot be dispatched by hand.",
                    "type": "boolean"
                },
                "interval": {
                    "description": "How often the group runs, e.g. \"30 minutes\", \"1 hour\", \"PT30M\", \"1800\". At least 60 seconds.",
                    "type": "string",
                    "minLength": 1
                },
                "latitude": {
                    "description": "Latitude of the location the forecast is fetched for.",
                    "type": "number",
                    "minimum": -90,
                    "maximum": 90
                },
                "longitude": {
                    "description": "Longitude of the location the forecast is fetched for.",
                    "type": "number",
                    "minimum": -180,
                    "maximum": 180
                },
                "units": {
                    "description": "Unit system used for temperatures.",
                    "type": "string",
                    "enum": [
                        "metric",
                        "imperial"
                    ]
                },
                "locale": {
                    "description": "Locale used for the 3-letter forecast day labels.",
                    "type": "string",
                    "enum": [
                        "fr",
                        "en"
                    ]
                },
                "staleAfter": {
                    "$ref": "#/definitions/staleAfter"
                },
                "staleBehavior": {
                    "description": "What the device does with the app once stale. \"hide\" falls back to the clock until the next push, \"none\" keeps showing the last forecast. Without it the firmware default applies.",
                    "type": [
                        "string",
                        "null"
                    ],
                    "enum": [
                        "hide",
                        "none",
                        null
                    ]
                },
                "activeWindow": {
                    "$ref": "#/definitions/activeWindow"
                }
            }
        },
        "trackerSync": {
            "description": "Assets fetched in one batch from a single provider.",
            "type": "object",
            "required": [
                "enabled",
                "interval",
                "items"
            ],
            "additionalProperties": false,
            "properties": {
                "enabled": {
                    "description": "A disabled group is never scheduled and cannot be dispatched by hand.",
                    "type": "boolean"
                },
                "interval": {
                    "description": "How often the group runs, e.g. \"30 minutes\", \"1 hour\", \"PT30M\", \"1800\". At least 60 seconds.",
                    "type": "string",
                    "minLength": 1
                },
                "items": {
                    "description": "Assets tracked in this group.",
                    "type": "array",
                    "minItems": 1,
                    "items": {
                        "$ref": "#/definitions/trackerItem"
                    }
                },
                "staleAfter": {
                    "$ref": "#/definitions/staleAfter"
                },
                "staleBehavior": {
                    "description": "What the device does with the app once stale. \"hide\" leaves the rotation until the next push, \"dim\" stays visible dimmed with a STALE badge, \"badge\" stays at full colour with the badge, \"none\" shows no sign. Without it the firmware default applies.",
                    "type": [
                        "string",
                        "null"
                    ],
                    "enum": [
                        "hide",
                        "dim",
                        "badge",
                        "none",
                        null
                    ]
                },
                "activeWindow": {
                    "$ref": "#/definitions/activeWindow"
                }
            }
        },
        "claudeSync": {
            "description": "Claude Code subscription counters pushed as one gauge. The OAuth credentials are read from the file named by PIXELCAST_CLAUDE_CREDENTIALS_FILE, never from this file.",
            "type": "object",
            "required": [
                "enabled",
                "interval"
            ],
            "additionalProperties": false,
            "properties": {
                "enabled": {
                    "description": "A disabled group is never scheduled and cannot be dispatched by hand.",
                    "type": "boolean"
                },
                "interval": {
                    "description": "How often the group runs, e.g. \"30 minutes\", \"1 hour\", \"PT30M\", \"1800\". At least 60 seconds.",
                    "type": "string",
                    "minLength": 1
                },
                "hiddenRows": {
                    "description": "Row labels left out of the gauge, as shown on the device (\"5h\", \"7j\", \"fable\", \"credits\"). A row named here is skipped even when the API answer carries it. Hiding every row leaves the group pushing nothing rather than an empty gauge. Without it every row the answer carries is shown.",
                    "type": "array",
                    "uniqueItems": true,
                    "items": {
                        "type": "string",
                        "enum": [
                            "5h",
                            "7j",
                            "fable",
                            "credits"
                        ]
                    }
                },
                "staleAfter": {
                    "$ref": "#/definitions/staleAfter"
                },
                "staleBehavior": {
                    "description": "What the device does with the app once stale. \"hide\" leaves the rotation until the next push, \"dim\" stays visible dimmed with a STALE badge, \"badge\" stays at full colour with the badge, \"none\" shows no sign. Without it the firmware default applies.",
                    "type": [
                        "string",
                        "null"
                    ],
                    "enum": [
                        "hide",
                        "dim",
                        "badge",
                        "none",
                        null
                    ]
                },
                "activeWindow": {
                    "$ref": "#/definitions/activeWindow"
                }
            }
        },
        "githubSync": {
            "description": "Pull requests waiting for a review, counted by one search query and pushed as one two-zone custom app. The token is read from PIXELCAST_GITHUB_TOKEN, never from this file.",
            "type": "object",
            "required": [
                "enabled",
                "interval",
                "query",
                "label"
            ],
            "additionalProperties": false,
            "properties": {
                "enabled": {
                    "description": "A disabled group is never scheduled and cannot be dispatched by hand.",
                    "type": "boolean"
                },
                "interval": {
                    "description": "How often the group runs, e.g. \"30 minutes\", \"1 hour\", \"PT30M\", \"1800\". At least 60 seconds.",
                    "type": "string",
                    "minLength": 1
                },
                "query": {
                    "description": "Search query the count comes from, e.g. \"is:open is:pr review-requested:@me\". Sent as it stands to the GitHub search endpoint, so a query GitHub refuses is only seen when the group runs.",
                    "type": "string",
                    "minLength": 1
                },
                "label": {
                    "description": "Text filling the lower band of the app, 31 characters at most. The band above already carries the count and its unit, \"3 PRs\" or \"1 PR\", so this one says what is to be done with them.",
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 31
                },
                "icon": {
                    "description": "Icon drawn next to the count, by the name the device knows it under. Without it the app shows the \"github\" icon.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "color": {
                    "description": "Colour of the label drawn under the count, as a hex code. Without it the label is drawn in the grey #7C9CB0. The count itself is not configurable: it is tinted by how many reviews wait, green up to 2, yellow up to 5, red beyond.",
                    "type": [
                        "string",
                        "null"
                    ],
                    "pattern": "^#[0-9a-fA-F]{6}$"
                },
                "staleAfter": {
                    "$ref": "#/definitions/staleAfter"
                },
                "staleBehavior": {
                    "description": "What the device does with the app once stale. \"hide\" leaves the rotation until the next push, \"none\" keeps showing the last count. Without it the firmware default applies.",
                    "type": [
                        "string",
                        "null"
                    ],
                    "enum": [
                        "hide",
                        "none",
                        null
                    ]
                },
                "activeWindow": {
                    "$ref": "#/definitions/activeWindow"
                }
            }
        },
        "staleAfter": {
            "description": "Silence tolerated by the device before the app is considered stale, in seconds. 0 = never. Without it the value is three times the interval, the same rule the healthcheck applies.",
            "type": [
                "integer",
                "null"
            ],
            "minimum": 0,
            "maximum": 604800
        },
        "activeWindow": {
            "description": "Hours during which the group is scheduled and watched by the healthcheck. Without it the group runs around the clock.",
            "type": "object",
            "required": [
                "from",
                "to"
            ],
            "additionalProperties": false,
            "properties": {
                "days": {
                    "description": "Days the window is open. Without it every day is open.",
                    "type": "array",
                    "minItems": 1,
                    "uniqueItems": true,
                    "items": {
                        "type": "string",
                        "enum": [
                            "mon",
                            "tue",
                            "wed",
                            "thu",
                            "fri",
                            "sat",
                            "sun"
                        ]
                    }
                },
                "from": {
                    "description": "Time the window opens, as HH:MM. Inclusive.",
                    "type": "string",
                    "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$"
                },
                "to": {
                    "description": "Time the window closes, as HH:MM. Inclusive, and strictly after \"from\": a window spanning midnight is not supported.",
                    "type": "string",
                    "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$"
                },
                "timezone": {
                    "description": "Timezone the two times are read in, e.g. \"Europe/Paris\". Without it device.timezone applies, and one of the two is required. A market opens at a local hour, not at the hour of the container clock.",
                    "type": "string",
                    "minLength": 1
                }
            }
        },
        "trackerItem": {
            "type": "object",
            "required": [
                "symbol",
                "currency"
            ],
            "additionalProperties": false,
            "properties": {
                "symbol": {
                    "description": "Identifier of the asset for this provider.",
                    "type": "string",
                    "minLength": 1
                },
                "currency": {
                    "description": "Currency the asset price is quoted in.",
                    "type": "string",
                    "minLength": 1
                },
                "icon": {
                    "description": "Identifier of the icon shown next to the asset on the device.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "label": {
                    "description": "Text shown on the top row instead of the symbol derived from the provider identifier.",
                    "type": [
                        "string",
                        "null"
                    ],
                    "minLength": 1,
                    "maxLength": 31
                },
                "labelColor": {
                    "description": "Hex color of the label. Without it the label follows the trend color, like the sparkline.",
                    "type": [
                        "string",
                        "null"
                    ],
                    "pattern": "^#[0-9a-fA-F]{6}$"
                },
                "bottomText": {
                    "description": "Text shown under the price.",
                    "type": [
                        "string",
                        "null"
                    ],
                    "minLength": 1,
                    "maxLength": 31
                },
                "bottomLine": {
                    "description": "What the row under the price shows when bottomText does not fix it. \"ath\" shows the all-time high of the asset, \"volume\" its 24 hour traded volume. Each group only accepts what it can serve: coingecko takes both, boursorama takes ath, twelvedata takes neither. Without it the group default applies: coingecko shows the volume, the other groups show nothing.",
                    "type": [
                        "string",
                        "null"
                    ],
                    "enum": [
                        "ath",
                        "volume",
                        null
                    ]
                },
                "volumeBars": {
                    "description": "Whether the device draws the traded volume as bars behind the price curve. Enabled without it, and only where the provider serves a volume for each point of the curve. Turning it off for a coingecko asset also spares the extra request that provider needs to collect the series.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "staleAfter": {
                    "$ref": "#/definitions/staleAfter"
                },
                "staleBehavior": {
                    "description": "What the device does with this asset once stale. \"hide\" leaves the rotation until the next push, \"dim\" stays visible dimmed with a STALE badge, \"badge\" stays at full colour with the badge, \"none\" shows no sign. Without it the value of the group applies.",
                    "type": [
                        "string",
                        "null"
                    ],
                    "enum": [
                        "hide",
                        "dim",
                        "badge",
                        "none",
                        null
                    ]
                },
                "activeWindow": {
                    "$ref": "#/definitions/activeWindow"
                }
            }
        }
    }
}
