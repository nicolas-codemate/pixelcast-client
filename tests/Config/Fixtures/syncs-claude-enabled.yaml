# Sample configuration used by ClaudeUsageProviderTest and TrackerAllTimeHighCommandTest: no tracker group at all.
syncs:
    claude:
        enabled: true
        interval: '15 minutes'
        staleBehavior: dim
