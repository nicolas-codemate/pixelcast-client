# Sample configuration used by ScheduleTest: the twelvedata group alone, to check the
# cadence it is scheduled at comes from this file.
syncs:
    twelvedata:
        enabled: true
        interval: '15 minutes'
        items:
            - symbol: AAPL
              currency: usd
              icon: stock
