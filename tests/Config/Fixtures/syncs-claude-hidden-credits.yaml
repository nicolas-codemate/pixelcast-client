# Sample configuration used by ClaudeUsageProviderTest: the credits row is hidden.
syncs:
    claude:
        enabled: true
        interval: '15 minutes'
        staleBehavior: dim
        hiddenRows: [credits]
