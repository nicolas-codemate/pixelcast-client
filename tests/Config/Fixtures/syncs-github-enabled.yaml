# Sample configuration used by GitHubPullRequestProviderTest: neither icon nor colour declared,
# so the group defaults apply.
syncs:
    github:
        enabled: true
        interval: '5 minutes'
        query: 'is:open is:pr review-requested:@me'
        label: 'A relire'
        staleBehavior: hide
